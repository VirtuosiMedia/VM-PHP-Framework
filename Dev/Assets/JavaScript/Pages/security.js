/**
 * Handles all of the interactions for the security page
 */
window.addEvent('domready', function(){
	var tabs = new SimpleTabs($$('.tab'), {activeClassName: 'active', triggerEvent: 'click'});
});